{
  "name": "sudoku-solver-web",
  "version": "1.0.0",
  "description": "A Sudoku solver using Dancing Links (DLX) algorithm",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/SudokuSolver/Main.ts",
    "build": "bun build src/SudokuSolver/Main.ts --outdir=dist --minify",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
