{
  "name": "sudoku-solver-web",
  "version": "1.0.0",
  "description": "A Sudoku solver using Dancing Links (DLX) algorithm",
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.html",
    "build": "bun build src/Main.ts --outdir=dist --minify --format=iife --target=browser",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
